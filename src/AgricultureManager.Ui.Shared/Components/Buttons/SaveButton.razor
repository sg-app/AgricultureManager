<RadzenButton ButtonType="@(IsSubmitButton ? ButtonType.Submit : ButtonType.Button)"  ButtonStyle="ButtonStyle.Primary" Icon="save" Text="Speichern" Click="Click" Disabled="@Disabled" />
@code {
    [Parameter] public bool IsSubmitButton { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> Click { get; set; }
}
